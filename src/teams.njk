---
layout: base.njk
title: F1 2026 Teams
---

<header style="text-align: center; margin-bottom: 40px;">
    <h1>The 2026 Grid</h1>
    <p>11 Teams. 22 Drivers. One Goal.</p>
</header>

<div class="grid-3" style="display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 20px;">
    {# Use 'dictsort' to keep teams in alphabetical order #}
    {% for filename, team in teams | dictsort %}
    <div class="card" style="border-top: 6px solid {{ team.color }}; padding: 20px; background: #1f1f1f; border-radius: 8px; color: white;">
        <div style="display: flex; justify-content: space-between; align-items: center;">
            <h3 style="margin: 0; font-size: 1.5rem;">{{ team.name }}</h3>
            <span style="font-size: 0.8rem; background: #333; padding: 4px 8px; border-radius: 4px;">{{ team.base }}</span>
        </div>
        
        <p style="font-size: 0.95rem; line-height: 1.5; margin: 15px 0; color: #ccc;">{{ team.desc }}</p>
        
        <h4 style="border-bottom: 1px solid #444; padding-bottom: 5px; margin-bottom: 10px; font-size: 0.9rem; color: {{ team.color }};">DRIVERS</h4>
        <ul style="list-style: none; padding: 0; margin: 0;">
            {% for item in team.drivers %}
            <li style="padding: 5px 0; font-weight: bold; border-bottom: 1px solid #2a2a2a;">
                {{ item.driver }}
            </li>
            {% endfor %}
        </ul>
    </div>
    {% else %}
    <div style="grid-column: 1 / -1; text-align: center; padding: 50px;">
        <p>No teams found. Add your first team in the <a href="/admin/" style="color: red;">CMS Manager</a>!</p>
    </div>
    {% endfor %}
</div>