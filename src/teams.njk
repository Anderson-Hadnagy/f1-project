---
layout: base.njk
title: F1 2026 Teams
---

<header style="text-align: center;">
    <h1>The 2026 Grid</h1>
    <p>11 Teams. 22 Drivers. One Goal.</p>
</header>

<div class="grid-4">
    
    {# Loop through your teams data #}
    {% for filename, team in teams %}
    
    <div class="card" style="border-top: 4px solid {{ team.color }};">
        
        <div class="team-card-header">
            <h3>{{ team.name }}</h3>
            <span style="font-size: 0.9rem; color: #888;">Base: {{ team.base }}</span>
        </div>

        <h4 style="color: {{ team.color }}; margin-top: 15px; margin-bottom: 5px; font-size: 0.8rem;">DRIVERS</h4>
        
        <ul class="drivers-list">
            {% for item in team.drivers %}
            <li>
                {# FIX: Check if it's an Object (CMS) or just Text (JSON) #}
                {% if item.driver %}
                    {{ item.driver }}
                {% else %}
                    {{ item }}
                {% endif %}
            </li>
            {% endfor %}
        </ul>

        <p style="font-size: 0.9rem; margin-top: 15px; color: #ccc; line-height: 1.5;">
            {{ team.desc }}
        </p>
    </div>

    {% else %}
    
    <div style="grid-column: 1 / -1; text-align: center; padding: 50px;">
        <h3>Waiting for Data...</h3>
        <p>Go to the <a href="/admin/" style="color: red;">CMS Admin</a> to add your teams.</p>
    </div>

    {% endfor %}

</div>